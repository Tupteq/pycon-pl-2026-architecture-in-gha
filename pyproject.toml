[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ci-patterns-demo"
version = "0.0.0"
description = "Workshop demo package for 'Software Architecture Meets CI/CD'"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
